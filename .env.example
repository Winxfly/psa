# PSA Service Environment Configuration
#
# Copy this file to .env and fill in your actual values.
# 
# Security Notice:
# - Values marked with [REQUIRED] must be filled
# - Values marked with [OPTIONAL] can be left empty
# - Default values will be used for optional fields if empty
#

# HeadHunter API Configuration
# Register your application at https://dev.hh.ru to get credentials
#
HH_CLIENT_ID=your_client_id_here                    # [REQUIRED] HeadHunter OAuth2 Client ID
HH_CLIENT_SECRET=your_client_secret_here            # [REQUIRED] HeadHunter OAuth2 Client Secret
HH_USER_AGENT=YourAppName (your-email@example.com)  # [REQUIRED] User-Agent for HH API requests
HH_ACCESS_TOKEN=                                    # [OPTIONAL] Pre-generated access token (will be auto-refreshed if empty)

# Database Configuration (PostgreSQL)
#
DB_USERNAME=psalocal                                # [REQUIRED] Database username
DB_PASSWORD=psalocal                                # [REQUIRED] Database password
DB_HOST=postgres                                    # [REQUIRED] Database host
DB_PORT=5432                                        # [REQUIRED] Database port
DB_NAME=psalocal                                    # [REQUIRED] Database name

# Redis Configuration
#
REDIS_ADDR=redis:6379                               # [REQUIRED] Redis server address
REDIS_PASSWORD=                                     # [OPTIONAL] Redis password
REDIS_DB=0                                          # [REQUIRED] Redis database number
REDIS_DEFAULT_TTL=24h                               # [REQUIRED] Default TTL for Redis keys (e.g., 24h, 60m)

# JWT Configuration
#
JWT_SECRET=your-super-secret-jwt-key-min-32-chars   # [REQUIRED] JWT signing secret
JWT_ACCESS_TOKEN_TTL=15m                            # [OPTIONAL] Access token TTL (default: 15m)
JWT_REFRESH_TOKEN_TTL=168h                          # [OPTIONAL] Refresh token TTL (default: 168h = 7 days)
JWT_ISSUER=psa-service                              # [OPTIONAL] JWT issuer (default: psa-service)

# Application Configuration
#
CONFIG_PATH=./config/local.yaml                     # [OPTIONAL] Path to YAML config file (default: ./config/local.yaml)